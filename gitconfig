# delta as the git diff pager - include from ~/.gitconfig
[core]
    pager = delta
[interactive]
    diffFilter = delta --color-only
[delta]
    navigate = true
    line-numbers = true
    pager = less -R --mouse
# diff colors - from the active theme
[include]
    path = ~/.config/dotfiles/active/delta.gitconfig
